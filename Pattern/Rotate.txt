class Solution {
    public void rotate(int[] nums, int k) {
        int n = nums.length-1;
        int temp;
        for(int i=0;i<=n;i++){
            temp = nums[i+1];
            nums[i+1]=nums[i];
            nums[i]=nums[n];
            nums
        }
    }
}